# authz-api

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build authz-api` to build the library.

## Running unit tests

Run `nx test authz-api` to execute the unit tests via [Jest](https://jestjs.io).
