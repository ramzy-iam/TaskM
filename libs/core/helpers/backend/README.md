# core-helpers-backend

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build core-helpers-backend` to build the library.

## Running unit tests

Run `nx test core-helpers-backend` to execute the unit tests via [Jest](https://jestjs.io).
