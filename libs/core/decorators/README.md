# core-decorators

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build core-decorators` to build the library.

## Running unit tests

Run `nx test core-decorators` to execute the unit tests via [Jest](https://jestjs.io).
